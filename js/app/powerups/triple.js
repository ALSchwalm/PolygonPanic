/**
 * A module which defines the player object
 * @module app/powerups/triple
 */
define(["app/config"], function(config){
   "use strict"

    var Triple = function(){
    };

    Triple.prototype.init = function() {
        
    }

    Triple.prototype.attack : function(unit) {

    }

    return Triple;
});
